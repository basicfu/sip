spring:
  application:
    name: sip-base
  profiles:
    active: ${ENV:dev}
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
sip:
  logs:
    redis:
      host: redis-dev.dmka.cn
      port: 4379
      password: root
